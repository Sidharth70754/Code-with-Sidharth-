# cache return values
# implement a decorator that caches the return value of a function, so that when it's called with the same arguments, the cached value is returned instead of re-executing the function.


